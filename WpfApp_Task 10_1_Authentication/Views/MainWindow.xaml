<Window x:Class="WpfApp_Task_10_1_Authentication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp_Task_10_1_Authentication"
        xmlns:converters="clr-namespace:WpfApp_Task_10_1_Authentication.Converters"
        xmlns:vm="clr-namespace:WpfApp_Task_10_1_Authentication.ViewModels"
        mc:Ignorable="d"
Title="Аутентификация" Height="250" Width="400"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:AuthViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <converters:BoolToColorConverter x:Key="BoolToColorConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Логин -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Логин:" Margin="0,0,10,10" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,0,0,10" VerticalAlignment="Center"/>

        <!-- Пароль -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Пароль:" Margin="0,0,10,10" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,0,0,10" VerticalAlignment="Center"/>

        <!-- Кнопка входа -->
        <Button Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Content="Войти" 
                Command="{Binding LoginCommand}" Margin="0,0,0,20" Height="30"
                HorizontalAlignment="Center" Width="100"/>

        <!-- Сообщение статуса -->
        <Border Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" 
                Background="{Binding IsSuccess, Converter={StaticResource BoolToColorConverter}}"
                CornerRadius="5" Padding="10" Margin="0,0,0,10">
            <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" HorizontalAlignment="Center"
                       FontWeight="Bold"/>
        </Border>
    </Grid>
</Window>